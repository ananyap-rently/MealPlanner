<div class="role-selection-wrapper">
  <div class="card shadow p-5" style="width: 100%; max-width: 1000px;">
    <h2 class="text-center mb-4">Choose Your Account Type</h2>
    <p class="text-center text-muted mb-5">Select the type of account that best suits your needs</p>

    <%= form_with url: role_selection_path, method: :post, local: true do |f| %>
      <div class="role-container">
        
        <label class="card-label">
          <%= f.radio_button :role, 'standard', id: 'role_standard' %>
          <div class="role-card card-body text-center">
            <span class="user-icon">👤</span>
            <h4 class="card-title">Standard User</h4>
            <p class="text-muted small">Perfect for casual users</p>
            <ul class="list-unstyled text-start mb-4">
              <li>✓ Browse recipes</li>
              <li>✓ Create meal plans</li>
              <li>✓ Shopping lists</li>
            </ul>
            <div class="select-btn btn btn-outline-primary w-100">Select Standard</div>
          </div>
        </label>

        <label class="card-label">
          <%= f.radio_button :role, 'premium', id: 'role_premium' %>
          <div class="role-card card-body text-center border-warning">
            <span class="crown-icon">👑</span>
            <h4 class="card-title">Premium User</h4>
            <span class="badge bg-warning text-dark mb-3">Recommended</span>
            <ul class="list-unstyled text-start mb-4">
              <li>✓ All standard features</li>
              <li>✓ Dashboard access</li>
              <li>✓ Priority support</li>
            </ul>
            <div class="select-btn btn btn-warning w-100">Select Premium</div>
          </div>
        </label>

      </div>

      <div class="text-center mt-5">
        <%= f.submit "Continue", class: "btn btn-lg btn-primary px-5" %>
      </div>
    <% end %>
  </div>
</div>